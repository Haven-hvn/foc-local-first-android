// Standalone module scaffold — copy this include block into your app's settings.gradle
// or embed foc-cache/ as a subdirectory of your existing multi-module Android project.

rootProject.name = "foc-local-first-android"

include(":foc-cache")
